// Global test setup. @testing-library/react-native v13+ registers its own
// jest matchers automatically, so nothing is needed for those.
// Add shared mocks (e.g. jest.mock('expo-router', ...)) here as the app grows.

export {};
